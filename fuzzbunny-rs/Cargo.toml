[package]
name = "fuzzbunny-rs"
version = "0.1.0"
edition = "2021"
license = "MIT"

[features]
default = []
std = []
rayon = ["std", "dep:rayon"]

[dependencies]
rayon = { version = "^1.11", optional = true }
